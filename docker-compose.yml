services:
  react-app:
    image: nginx:alpine
    volumes:
      - ./build:/usr/share/nginx/html
    ports:
      - "8080:80"  # Map container's port 80 to host's port 8080
    restart: unless-stopped
